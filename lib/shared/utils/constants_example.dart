/// Use this variable to setup which enviroment you are using this app
/// I suggest to use [env] with the following values: `d` for development,
/// `h` for homolog, and `p` or empty value for production environment.
/// ```dart
/// const String env = 'd';
/// ```
const String env = 'd';

/// This variable refers at the base url to use the Marvel APIs.
const String baseUrl = '';

/// This variable represent your secret apiKey to use the Marvel APIs as developer.
/// Keep sure that you not using [constants_example.dart] file for the App.
/// The correctly use is copy the example file to a new file without `_example` in name.
const String publicKey = '';
